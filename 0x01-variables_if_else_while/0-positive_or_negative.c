#include <studlio.h>
#include <time.h>

/**more header goes there
 *
 * betty style doc for fuction main goes there
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	/* your code to return to 0*/
	return (0);
}
