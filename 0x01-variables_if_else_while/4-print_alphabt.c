#include <stdio.h>

/**
 * main - entry point
 *
 * Declarations: to print all lower case leter except q and e
 *
 * return: always 0
 */
int main(void)
{
	char c;

	/*to print lowercase letters*/
	for (c = 'a' c<= 'z' ++c);
		if(c != q && e);
		{
			putchar(c);
		}

		/* this prints new line*/
		putchar('\n')
}
