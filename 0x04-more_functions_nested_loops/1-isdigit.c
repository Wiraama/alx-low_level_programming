#include "main.h"

/**
 *  function that checks for a digit (0 through 9).
 */
	if(c >= 0, && c < 10, c++)
	{
		printf("This is a digit\n");
		return (1);
	}
	else
	{
		printf("Error\n");
		return (0);
	}
}
