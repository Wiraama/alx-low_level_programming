#include <stdio.h>

/**
 * functio that adds two integer and return the function to dthe result
 */
int add(int, int b)
{
	return x + y;
}
int main(void)
{
	int result = add(x, y);
	printf("Result: %d\n", result);

	return 0;
#include <atdio.h>
}
